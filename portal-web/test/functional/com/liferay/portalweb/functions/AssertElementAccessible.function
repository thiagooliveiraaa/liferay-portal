@default = "assertElementAccessible"
definition {

	function assertElementAccessible {
		WaitForSPARefresh();

		selenium.assertElementAccessible();

		selenium.assertJavaScriptErrors();

		selenium.assertLiferayErrors();
	}

}