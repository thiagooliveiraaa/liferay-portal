@component-name = "portal-frontend-infrastructure"
definition {

	property custom.properties = "feature.flag.LPS-164563=true";
	property portal.acceptance = "true";
	property portal.release = "true";
	property portal.upstream = "true";
	property testray.component.names = "Frontend Dataset";
	property testray.main.component.name = "Frontend Dataset";

	setUp {
		TestCase.setUpPortalInstance();

		User.firstLoginPG();
	}

	tearDown {
		var testPortalInstance = PropsUtil.get("test.portal.instance");

		FrontendDataSetAdmin.deleteAllDatasetEntries();

		if (${testPortalInstance} == "true") {
			PortalInstances.tearDownCP();
		}
	}

	@description = "LPS-186434. Confirm that the Delete option is present."
	@ignore = "true"
	@priority = 3
	test CanAssertDeleteIsPresent {

		// TODO LPS-186434 CanAssertDeleteIsPresent pending implementation

	}

	@description = "LPS-186435. Confirm that the field is deleted and a successful message is displayed."
	@ignore = "true"
	@priority = 3
	test CanAssertFilterIsDeleted {

		// TODO LPS-186435 CanAssertFilterIsDeleted pending implementation

	}

	@description = "LPS-186431. Confirm that an element dragged and dropped below another,will be deprioritized."
	@ignore = "true"
	@priority = 5
	test CanBeDeprioritizedBelowAnother {

		// TODO LPS-186431 CanBeDeprioritizedBelowAnother pending implementation

	}

	@description = "LPS-186430. Confirm that an element dragged and dropped on top of another element will be prioritized."
	@ignore = "true"
	@priority = 4
	test CanBePrioritizedOverAnother {

		// TODO LPS-186430 CanBePrioritizedOverAnother pending implementation

	}

	@description = "LPS-186437. Confirm that the field is not deleted by clicking the Cancel button."
	@ignore = "true"
	@priority = 5
	test CanCancelButtonDeleteFilter {

		// TODO LPS-186437 CanCancelButtonDeleteFilter pending implementation

	}

	@description = "LPS-186429. Confirm that the elements of the list can be dragged and dropped."
	@ignore = "true"
	@priority = 4
	test CanInteractDragDrop {

		// TODO LPS-186429 CanInteractDragDrop pending implementation

	}

}