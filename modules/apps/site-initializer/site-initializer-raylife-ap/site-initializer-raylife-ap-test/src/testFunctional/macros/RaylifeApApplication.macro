definition {

	macro assertModalInfos {
		if (isSet(option)){
		AssertElementPresent(
			key_text = ${option},
			locator1 = "RaylifeApplicationModal#MODAL_INSURANCE");
		}
		if (isSet(notOption)) {
			AssertElementNotPresent(
				key_text = ${notOption},
				locator1 = "RaylifeApplicationModal#MODAL_INSURANCE");
		}
		if (isSet(description)) {
			AssertElementPresent(
				key_text = "${description}",
				locator1 = "RaylifeApplicationModal#MODAL_DESCRIPTION");
		}
	}

	macro modalFlow {
		Button.click(button = "Application");

		RaylifeApApplication.modalOption(
			key_text = ${insurance},
			locator1 = "RaylifeApplicationModal#MODAL_OPTION");

		Button.click(button = "Next");

		if (isSet(button)) {
			Button.click(button = "${button}");
		}

		if (isSet(product)) {
			RaylifeApApplication.modalOption(
				key_text = ${product},
				locator1 = "RaylifeApplicationModal#MODAL_OPTION");

			Button.click(button = "Next");
		}
	}

	macro modalOption {
		Click(
			key_text = ${option},
			locator1 = "RaylifeApplicationModal#MODAL_OPTION");
	}

}